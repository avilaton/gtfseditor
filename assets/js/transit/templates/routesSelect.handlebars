<label for="route">Route
	<select id="route">
	  <option value=""> -- </option>
	  {{#each routes}}
	    <option value={{route_id}}>{{route_type}} {{route_short_name}}</option>
	  {{/each}}
	</select>
</label>
