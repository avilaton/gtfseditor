require([
    "config"
    ],
    function (config) {
    'use strict';

    var app = window.app = {};

    // app.state = new StateModel();

    // init.createControls();
    console.log("we have liftoff");

});
